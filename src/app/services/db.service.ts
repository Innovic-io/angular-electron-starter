// import * as path from 'path';
// import * as fs from 'fs';

export class DatabaseService {

  getState() {}

  setState(state) {
    /*
    fs.appendFile(path.normalize('./.data.json'), '\n' + JSON.stringify(Object.assign({}, state)), () => {
      console.log('saved');
    });
    */
  }
}
